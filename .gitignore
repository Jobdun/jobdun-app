# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Google OAuth secrets — never commit these
ios/Runner/GoogleService-Info.plist
android/app/google-services.json
client_secret_*.json
client_*.plist

# Environment and local secret files
.env
.env.*
!.env.example
scripts/.ship-env
scripts/.cloudflare-env
*.local
*.secret
*.secrets

# Wrangler / Cloudflare Pages local state + logs
.wrangler/

# Private keys, certificates, signing assets
*.pem
*.key
*.p8
*.p12
*.jks
*.keystore
*.mobileprovision
*.cer
*.crt

# Local databases, dumps, and runtime state
*.db
*.sqlite
*.sqlite3
*.dump
*.pid
*.seed

# Logs and reports
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
firebase-debug.log*

# Tooling caches and local overrides
.firebase/
.turbo/
.vercel/
.parcel-cache/
*.orig

# Flutter golden-test failure artifacts (regenerated on every mismatch)
test/**/failures/

# Brainstorming visual-companion artifacts
.superpowers/

# Firebase service-account keys (NEVER commit — secret private key)
*firebase-adminsdk*.json

# Android release signing — NEVER commit. Backup drill: docs/RELEASE_SIGNING.md
android/key.properties
android/upload-keystore.jks

# Local screenshots from emulator runs (not for commit)
ss/
